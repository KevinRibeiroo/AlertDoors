# Example names only. The application does not load .env files automatically.
# Export variables in the shell or inject them through Cloud Run/Secret Manager.
ALERTDOORS_MODE=Production
GOOGLE_CLOUD_PROJECT=example-project
DISCORD_ENABLED=false
DISCORD_WEBHOOK_URL=
EMAIL_ENABLED=false
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=
EMAIL_TO=
SMTP_ALLOW_LOCAL_INSECURE=false
# Development only: ALERTDOORS_MODE=Development and FIRESTORE_EMULATOR_HOST=127.0.0.1:8080
